import React from 'react';
import ProjectList from './ProjectList';

const Projects = () => {
    return (
        <div>
            <ProjectList/>
        </div>
    );
}
export default Projects;
